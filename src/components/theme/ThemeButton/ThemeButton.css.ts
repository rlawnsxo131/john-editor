import { style } from '@vanilla-extract/css';

export const button = style({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  border: 0,
  background: 'none',
  padding: '0.5rem',
  cursor: 'pointer',
});
